#include <stdio.h>

int
main(int argc, char * argv[])
{
	int numofcommad, i;
	numofcommad = argc;
	printf("numofcommad = %d\n", numofcommad);
	for(i=0; i<argc; i++)
	{
		printf("arg[%d] = %s\n", i, argv[i]);
	}
	return 0;
}
